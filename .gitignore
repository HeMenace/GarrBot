node_modules
.env
.dev.vars
.wrangler
ngrok-traffic-policy.yml
dist

# Generated by `npm run render-wotd` from assets/wotd/base.png + data/words.txt.
# CI regenerates these on every deploy, so they don't need to be committed.
public/wotd/*.png
assets/wotd/preview.png
